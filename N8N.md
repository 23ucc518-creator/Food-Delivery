# 🤖 NL-to-SQL Workflow (n8n)

As an extension to the SQL analysis, a **natural language to SQL** workflow was built in n8n — allowing business questions to be asked in plain English and answered directly from the MySQL database, without writing SQL manually.

## ⚙️ How It Works

The workflow consists of 4 connected nodes:

| Node | Role |
|---|---|
| **When chat message received** | Trigger — captures the user's natural language question |
| **AI Agent** | Core orchestrator — interprets the question and decides what SQL to run |
| **Google Gemini Chat Model** | LLM powering the AI Agent's reasoning and query generation |
| **Execute a SQL query in MySQL** | Tool — runs the AI-generated SQL directly against the `fooddelivery` database and returns results to the agent |

**Flow:** User asks a question in chat → AI Agent (using Gemini) converts it into a SQL query based on the database schema → the SQL tool executes it against MySQL → Gemini formats the raw result into a clear, human-readable answer → response is returned in chat.

[n8n Workflow]

<img width="1905" height="926" alt="image" src="https://github.com/user-attachments/assets/c6009dba-c4ad-4c8a-85b3-2dc009cc2157" />


## 🧪 Sample Queries & Results

These are actual outputs generated by the workflow, matching results independently verified in the manual SQL analysis (see [SQL.md](./SQL.md)).



**1. Of all orders successfully fulfilled by the restaurant, what percentage were ultimately delivered successfully vs. failed during delivery?**

<img width="800" height="400" alt="image" src="https://github.com/user-attachments/assets/81623319-3c42-4e5f-a61d-907a69479dd0" />


✅ Matches the manually written SQL result for the same question.


**2. What are peak order days (day of week)?**

<img width="800" height="400" alt="image" src="https://github.com/user-attachments/assets/acee87c8-6470-4909-85c9-7913303355ee" />


✅ Matches the manually written SQL result for the same question.

**3. Who are the customers that churned between 2023 and 2024, based on having ordered in 2023 with no activity since?**

<img width="800" height="400" alt="image" src="https://github.com/user-attachments/assets/5d846e20-af7e-4708-bd7c-f9f87c2de1c3" />



✅ Matches the manually written SQL result for the same question.

**4. What is the most frequently ordered dish in each city?**

<img width="800" height="400" alt="image" src="https://github.com/user-attachments/assets/c24aeaa8-3ac2-42be-9f86-b3323d9c4d34" />


✅ Matches the manually written SQL result for the same question.

**5. Rank each city based on the total revenue ?**

<img width="1113" height="470" alt="image" src="https://github.com/user-attachments/assets/681304cb-b121-4908-bad1-f2149169319d" />



## ✅ Why This Matters

Each of these AI-generated results was cross-checked against the equivalent hand-written SQL query from the core analysis — confirming the AI Agent correctly interprets business questions, applies the right joins/filters, and produces accurate results without manual query writing. This demonstrates an applied GenAI layer on top of a traditional SQL analytics project — turning a static query file into an interactive, natural-language BI assistant.
